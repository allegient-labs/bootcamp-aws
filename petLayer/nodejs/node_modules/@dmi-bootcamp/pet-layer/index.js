const AWS = require('aws-sdk');
const Pet = require('./Pet');

module.exports = {
    Pet,
    dynamo: new AWS.DynamoDB.DocumentClient(),
    createResponse: (statusCode, body) => ({ statusCode, body: JSON.stringify(body) }),
};